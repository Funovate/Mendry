---
import DefaultPageTitle from "@astrojs/starlight/components/PageTitle.astro";

const introductionRoute = Astro.url.pathname === "/" || Astro.url.pathname === "/zh-cn/";
---

{!introductionRoute && <DefaultPageTitle />}
